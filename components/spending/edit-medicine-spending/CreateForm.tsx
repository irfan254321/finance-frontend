/**
 * @component CreateForm
 * @description Placeholder for CreateForm.
 * Medicine creation is handled via Spending Input, not directly here.
 */

"use client";

import { Button } from "@mui/material";

export default function CreateForm() {
  return (
    <div className="hidden">
      {/* Placeholder to satisfy the 4-file structure requirement */}
      <Button>Create Medicine</Button>
    </div>
  );
}
